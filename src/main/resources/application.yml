server:
  port: 8081
    
spring:
  jpa:
   show-sql: true
   hibernate:
      ddl-auto: create
      
  datasource:
    url: jdbc:mysql://localhost:3306/bookkeeping
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
